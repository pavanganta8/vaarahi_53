name: 'dbt_snowflake'
version: '1.0.0'
config-version: 2

profile: 'dbt_snowflake'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_snowflake:
    staging:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
